# macOS metadata files
._*
.DS_Store

# Raw large source files — not needed for the deployed site
# (web-optimised versions live in assets/web/ and are tracked)
assets/*.mov
assets/*.MOV
assets/*.mp4
assets/*.MP4
assets/*.pdf
!assets/Ben-Jeffreys-CV.pdf

# Duplicate staging folder — site is served from repo root
site/

# Editable CV source (the published PDF lives in assets/)
*.docx

# Unused scan exports
assets/web/_pdfcheck-*.jpg
assets/web/_scan-*.jpg
